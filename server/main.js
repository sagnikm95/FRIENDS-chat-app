import { Meteor } from 'meteor/meteor';

import '../imports/api/chat.js';
import '../imports/api/message.js';
import '../imports/api/broadcastMessage.js';

Meteor.startup(() => {
  // code to run on server at startup
});
